import Foundation

/// SF Symbol name constants. Feature views should reference these rather
/// than hardcoding symbol name string literals.
enum Icon {
    static let home = "house.fill"
    static let timeline = "list.bullet.rectangle"
    static let charts = "chart.xyaxis.line"
    static let settings = "gearshape.fill"
    static let add = "plus"
    static let temperature = "thermometer"
}
