// TODO(Phase 5, FeverLogEngine): Implement DoseCalculator, MedicationSafetyEngine,
// MedicationRules, RollingWindow, and MedicationGrouping.
// Completion: engine module provides deterministic dose/safety calculations with full test coverage.
// Release blocker: yes for medication logging.
//
// This module must never import SwiftUI, SwiftData, Supabase, UIKit, or app view models.

public enum FeverLogEngineInfo {
    public static let version = "0.1.0"
}
