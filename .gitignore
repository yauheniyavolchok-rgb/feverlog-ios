# Xcode
build/
DerivedData/
*.xcodeproj
!default.xcodeproj
xcuserdata/
*.xcscmblueprint
*.xccheckout
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# XcodeGen output (regenerated via `xcodegen generate`)
*.xcworkspace

# CocoaPods (unused, kept for safety)
Pods/

# macOS
.DS_Store

# Secrets — never commit real values, only .env.example
.env
Secrets.xcconfig

# Fastlane (if added later)
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots/**/*.png
fastlane/test_output
